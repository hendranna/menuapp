<%= simple_form_for(@menu) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <%= f.collection_select(:dish_ids, @dishes,
:id,:name)%>
  
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
