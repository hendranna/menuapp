<p id="notice"><%= notice %></p>

<p>
  <h1>Name</h1>
  <%= @menu.name %>
  <h2>Dishes</h2>
  <% @menu.dishes.each do |dish| %>
  	<%= dish.name %>
  	<% end %>
</p>


<%= link_to 'Edit', edit_menu_path(@menu) %> |
<%= link_to 'Back', menus_path %>
